import 'package:flutter/material.dart';
import 'package:velo/features/auth/presentation/widgets/done_reset_pass_view_body.dart';

class DoneResetPassView extends StatelessWidget {
  const DoneResetPassView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const DoneResetPassViewBody());
  }
}
